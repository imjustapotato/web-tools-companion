{
  "name": "web-tools-helper",
  "version": "1.0.2",
  "description": "A companion extension for my Web Tools website.",
  "main": "index.js",
  "scripts": {
    "dev": "node build.mjs",
    "build": "node build.mjs",
    "build:store": "node build.mjs --package",
    "build:firefox": "node build.mjs --package --firefox",
    "build:obfuscate": "node build.mjs --package --obfuscate",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Kenneth Westhle A. Davila",
  "license": "GPL-3.0-or-later",
  "type": "commonjs",
  "devDependencies": {
    "@types/chrome": "^0.1.40",
    "archiver": "^7.0.1",
    "esbuild": "^0.28.0",
    "terser": "^5.46.1",
    "vite": "^8.0.8"
  },
  "dependencies": {
    "gsap": "^3.15.0",
    "iconify-icon": "^3.0.2"
  }
}
